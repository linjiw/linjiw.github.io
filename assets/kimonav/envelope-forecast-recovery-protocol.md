# Forecast-only recovery of the reference-envelope grid

The original18 geometry attempts completed before any forecast:16 admitted,
2 rejected. The first current model attempt stopped at the nominal actor's
package-identity guard, with zero callback/persisted states. The geometry
orchestrator pinned the live telemetry runtime; the frozen nominal CPU actor
requires its separate original continuation runtime. Both trees were already
source-bound. No weights, model equations, reference or task change is needed.

Preserve the entire original output, including its failed model attempt and
19 unattempted forecast slots. Reuse all18 saved geometry results; do not run
another optimizer or converter. In a separate CPU process, pin the nominal
actor's required import tree and verify the actual actor module path before
launch. Bind the original geometry, failed attempt, unchanged source closure,
new orchestration and passing package-identity tests in a new receipt.

Allocate one declared recovery of left-current, followed by eligible left grid
references, right-current and eligible right references in original order.
All20 slots remain:18 model attempts and2 explicit reference rejections.
No model outcomes have been produced or inspected in this grid. This amendment
uses geometry eligibility exactly as originally specified, with no score-based
choice, new geometry, extra baseline, calibration or changed horizon. A model
integration failure remains a failed forecast; infrastructure failure halts
later model attempts. No further recovery is authorized by this allocation.

Report18 completed geometry proposals and model-attempt accounting separately.
The overall research report retains the initial zero-state infrastructure
failure even if this completion succeeds. No robot simulation, generation or
final36 access occurs in this forecast-only recovery. The final saved-output
audit joins original geometry with the versioned completed model ledger and
independently checks raw projections, reference bounds and original-clock
scores. It does not rerun a producer.
